<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.antoinedrouin.enjoyfood">
    // Autoriser la connexion réseau
    <uses-permission android:name="android.permission.INTERNET" />
    // Localisation approximative
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    // Localisation précise
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/customTheme">
        <activity android:name=".Activities.Tabs">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_key" />

        <activity android:name=".Activities.Login" />
        <activity android:name=".Activities.Register" />
        <activity android:name=".Activities.Support" />
        <activity android:name=".Activities.Compte" />
        <activity android:name=".Activities.Etablissement" />
        <activity android:name=".Activities.Reglages" />
        <activity android:name=".Activities.MapPlacePicker" />
        <activity android:name=".Activities.EtablissementManager" />
        <activity android:name=".Activities.EtablissementManagerInfos" />
        <activity android:name=".Activities.EtablissementManagerInfosDetails" />
        <activity android:name=".Activities.Consommable" />
        <activity android:name=".Activities.PanierDetails"></activity>
    </application>

</manifest>